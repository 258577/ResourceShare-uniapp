{
    "bsonType": "object",
    "required": [
        "resource_id",
        "recommended_at"
    ],
    "permission": {
        "read": true,
        "create": "auth.role == 'admin'",
        "update": "auth.role == 'admin'",
        "delete": "auth.role == 'admin'"
    },
    "properties": {
        "_id": {
            "description": "ID，系统自动生成"
        },
        "resource_id": {
            "bsonType": "string",
            "description": "资源ID"
        },
        "recommended_at": {
            "bsonType": "timestamp",
            "description": "推荐时间"
        },
        "create_date": {
            "bsonType": "timestamp",
            "description": "创建时间"
        }
    }
}